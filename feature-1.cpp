#include <bits/stdc++.h>
using namespace std;

// multiplication has to be done 

int main() {

    // adding sort an array code 
    vector<int>v = {1,5,2,3};
    sort(v.begin(),v.end());

    // line added in branch latest from kabeer

    int x = 10;
    int y = 20;
    cout<<"Multiplication is :"<<x*y<<endl;
    return 0;
}
